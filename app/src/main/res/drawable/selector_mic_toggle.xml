<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Muted State (When button is toggled/selected to mute) -->
    <item android:state_selected="true" android:drawable="@drawable/mic_off_24px" />
    
    <!-- Active State (Default open microphone setting) -->
    <item android:drawable="@drawable/mic_24px" />
</selector>